spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/redaCode/SpringCloudConfig/
          search-paths: reda
          username: richie.shg@aliyun.com
          password: honggui5
      label: master
server:
  port: 8888
eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/